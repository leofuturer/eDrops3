import React from "react";

interface Props {
    id: number;
}

function Project({ id } : Props) {
    return (
        <div>
            <div></div>
        </div>
    );
}

export default Project;
