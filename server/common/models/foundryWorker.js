'use strict';

module.exports = function(Foundryworker) {

  //Worker instance retrieves orders belongs to him
  //Using: GET /foundryWorkers/{id}/orders (Filters can also be used at the same time)
  //Frontend needs to pass worker id to backend
  
};
